# Requirements for the Good Neighbor Agent (community waste recovery)
# Core AgentCore dependencies
bedrock-agentcore
# Pin Strands to a version with correct Amazon Nova tool-use loop handling.
# Older builds ended the agent loop after the first tool call (no synthesis
# turn), so the agent never turned tool data into an answer.
strands-agents==1.55.1
bedrock-agentcore-starter-toolkit==0.3.9
asyncio

mcp>=1.9.0,<2.0.0

# Additional dependencies for the workshop
boto3>=1.34.0
botocore>=1.34.0
requests>=2.31.0