# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# AWS SAM build output (regenerated by `sam build`)
.aws-sam/

# Temporary invoke/CLI output captured during testing
out.json
payload.json
q-*.json
*-out.txt
*-out*.json

# Secrets / credentials — never commit
.env
.env.*
*.env
**/secrets.json
# Local convenience script for setting gateway credentials (see gateway/README)
static/AgentCode/gateway/set-env.ps1
static/AgentCode/gateway/set-env.sh

# OS / editor noise
.DS_Store
Thumbs.db

# NOTE: static/AgentCode/.bedrock_agentcore.yaml is intentionally NOT ignored —
# it records the deployed AgentCore runtime config. Keep it in the repo.
